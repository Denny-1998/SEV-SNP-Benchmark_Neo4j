# terraform.tfvars.example — copy this to terraform.tfvars and fill in your own values.


project_id = "project_id"
zone       = "europe-west4-a"

# Uncomment the sizes you want to run this session.
#core_counts = [2]
core_counts = [2, 4, 8, 16]
